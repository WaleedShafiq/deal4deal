import { Campagin } from './campagin';

export class Cart {
  id: number;
  // tslint:disable-next-line:variable-name
  client_id: number;
  // tslint:disable-next-line:variable-name
  campaign_id: number;
  quantity: number;
  campaign: Campagin;
}
